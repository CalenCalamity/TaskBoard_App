import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TaskBoard_App',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
